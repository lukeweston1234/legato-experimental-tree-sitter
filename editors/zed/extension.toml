id = "legato"
name = "Legato"
version = "0.0.1"
schema_version = 1
authors = ["Luke <lukeweston1234@gmail.com>"]
description = "Syntax highlighting for the Legato DSL"
repository = "https://github.com/lukeweston1234/legato-experimental-tree-sitter"

# Zed fetches and builds the grammar from git; it does not support local paths.
# `rev` MUST be a commit that is pushed to the repository below and that contains
# the generated src/parser.c (i.e. after the `kernel` change is committed).
[grammars.legato]
repository = "https://github.com/lukeweston1234/legato-experimental-tree-sitter"
rev = "REPLACE_WITH_PUSHED_COMMIT_SHA"
